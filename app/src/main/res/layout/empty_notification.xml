<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:padding="20dp"
    android:layout_height="match_parent">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_centerInParent="true"
        android:layout_height="wrap_content">

        <ImageView
            android:layout_width="70dp"
            android:layout_height="70dp"
            android:layout_gravity="center"
            android:layout_margin="20dp"
            android:tint="@color/icon_grey"
            android:src="@drawable/ic_notifications_none_black_24dp" />

        <TextView
            android:layout_width="match_parent"
            android:textAlignment="center"
            android:padding="20dp"
            android:text="Hey,Currently You have no notification. We shall be notifying you when your order is accepted, deleted, saved and others like promotions, give aways"
            android:layout_height="wrap_content"/>

        <TextView
            android:id="@+id/shopNow"
            android:layout_weight="2"
            android:layout_width="match_parent"
            android:layout_height="35dp"
            android:layout_gravity="center"
            android:text="GO TO SHOP NOW"
            android:layout_alignParentBottom="true"
            android:layout_margin="30dp"
            android:gravity="center_vertical"
            android:paddingStart="20dp"
            android:drawablePadding="30dp"
            android:backgroundTint="@color/myBlue"
            android:drawableLeft="@drawable/ic_add_shopping_cart_black_24dp"
            android:textAllCaps="true"
            android:drawableTint="@android:color/white"
            android:textColor="@android:color/white"
            android:background="@drawable/borderless2"
            android:paddingLeft="20dp" />
    </LinearLayout>
</RelativeLayout>